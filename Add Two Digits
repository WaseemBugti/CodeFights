

You are given a two-digit integer n. Return the sum of its digits.


---------------------------------
Solution
---------------------------------

int addTwoDigits(int n) {    
if(n>=0){    
    int sum = 0;         
while(n> 0)
  {           
    int r = n%10; 
    sum += r;
    n /=10;
                 }
         
      return sum;
   } 
} 
